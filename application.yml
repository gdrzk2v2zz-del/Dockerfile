server:
  port: 8080

spring:
  main:
    banner-mode: "off"
  config:
    import: "optional:configserver:"

lavalink:
  server:
    password: "youshallnotpass"
    sources:
      youtube: true
      bandcamp: true

spring:
  cloud:
    config:
      enabled: false
